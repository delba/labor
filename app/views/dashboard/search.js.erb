$('ul.employees').remove();

<% if @employees.any? %>
  $('<ul class="employees">')
    .html('<%= j render partial: 'employee', collection: @employees %>')
    .insertAfter('#new_search');
<% end %>
